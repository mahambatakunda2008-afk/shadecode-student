# Native Room foundation

The native Android client now has local Room entities and DAOs for subjects, lesson progress, and queued sync operations. The Android build must provide the Room Gradle plugin and KSP compiler before this database is compiled.
