# Lesson generation production sync

This marker keeps the production Git integration synchronized with the current `main` tree after the resilient lesson-generation fix.

The lesson generation route is expected to include its deterministic resilience path and must be deployed from the current `main` commit.
