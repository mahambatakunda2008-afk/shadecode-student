"use client";

import katex from "katex";
import "katex/dist/katex.min.css";
import { useCallback, useEffect, useMemo, useRef, useState, type ReactNode } from "react";
import { AlertTriangle, ArrowLeft, ArrowRight, BookOpen, Calculator, Check, CheckCircle2, ChevronDown, ChevronLeft, ChevronRight, Clock3, CloudOff, Flag, FlagOff, Grid2X2, Lightbulb, Loader2, Menu, MoreHorizontal, Paperclip, Pencil, RotateCcw, Save, Sparkles, Wifi, X, Zap } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import { fetchWithTimeout, FetchTimeoutError } from "@/lib/async/fetchWithTimeout";
import { useCortexVerify } from "@/hooks/useCortexVerify";
import StudyCanvas from "@/components/studyspace/StudyCanvas";
import { createStudySession, deleteStudySession, getStudySession, saveStudySession } from "@/lib/studyspace/session";
import { saveWorkObject } from "@/lib/studyspace/store";
import type { StudySpaceMode } from "@/lib/studyspace/types";

export type ExamQuestion = { id: number; type: "multiple_choice" | "short_answer" | "structured"; question: string; options?: string[]; marks: number; topic: string };
export type ExamResult = { questionId: number; score: number; maxScore: number; correct: boolean; feedback: string; modelAnswer: string; topic: string };
export type ExamResults = { totalScore: number; maxScore: number; percentage: number; grade: string; weakAreas: string[]; strongAreas: string[]; cortexInsight: string; results: ExamResult[]; timeTaken: number };
type Answer = { questionId: number; answer: string; timeSpent: number };
type Props = { initialSubject?: string; initialTopic?: string; initialDifficulty?: number; initialQuestionCount?: number; onExit?: () => void; onFinished?: (result: ExamResults) => void };

const LEVELS = [{ label: "O-Level", api: "easy" as const, curriculum: "O-Level standard", accent: "#22c55e" }, { label: "A-Level", api: "medium" as const, curriculum: "A-Level standard", accent: "#06b6d4" }, { label: "University", api: "hard" as const, curriculum: "university entrance standard", accent: "#8b5cf6" }];
const SUBJECTS = ["Mathematics", "Physics", "Chemistry", "Biology", "Computer Science", "English", "Geography", "History", "Economics", "Accounting", "Business Studies", "Other"];
const COUNTS = [5, 10, 15, 20];
const KEY = "shadecode-exam-workspace";

function math(text: string) { try { return (text || "").replace(/\$\$([^$]+)\$\$/g, (_, e) => katex.renderToString(e, { displayMode: true, throwOnError: false })).replace(/\$([^$]+)\$\$/g, (_, e) => katex.renderToString(e, { displayMode: false, throwOnError: false })); } catch { return text; } }
function grade(p: number) { return p >= 90 ? "A*" : p >= 80 ? "A" : p >= 70 ? "B" : p >= 60 ? "C" : p >= 50 ? "D" : p >= 40 ? "E" : "U"; }
function time(s: number) { const n = Math.max(0, Math.floor(s)); const h = Math.floor(n / 3600); const m = Math.floor((n % 3600) / 60); const sec = n % 60; return h ? `${String(h).padStart(2, "0")}:${String(m).padStart(2, "0")}:${String(sec).padStart(2, "0")}` : `${String(m).padStart(2, "0")}:${String(sec).padStart(2, "0")}`; }
function id() { return `${Date.now()}-${crypto.randomUUID?.() ?? Math.random().toString(36).slice(2)}`; }
function formulas(subject: string) { const s = subject.toLowerCase(); if (s.includes("math")) return ["a² + b² = c²", "ax² + bx + c = 0", "sin²θ + cos²θ = 1", "gradient = Δy / Δx"]; if (s.includes("physics")) return ["v = u + at", "s = ut + ½at²", "F = ma", "p = mv", "V = IR"]; if (s.includes("chem")) return ["n = m/M", "c = n/V", "PV = nRT", "pH = −log₁₀[H⁺]"]; if (s.includes("computer")) return ["T(n) = O(n)", "binary search: O(log n)", "1 byte = 8 bits"]; return ["State definitions precisely.", "Show method where marks depend on it.", "Check units, assumptions and final statements."]; }

export default function ExamWorkspace({ initialSubject = "", initialTopic = "", initialDifficulty = 1, initialQuestionCount = 10, onExit, onFinished }: Props) {
  const [subject, setSubject] = useState(initialSubject), [topic, setTopic] = useState(initialTopic), [level, setLevel] = useState(Math.max(0, Math.min(2, initialDifficulty))), [count, setCount] = useState(initialQuestionCount);
  const [questions, setQuestions] = useState<ExamQuestion[]>([]), [answers, setAnswers] = useState<Answer[]>([]), [current, setCurrent] = useState(0), [seconds, setSeconds] = useState(0), [totalSeconds, setTotalSeconds] = useState(0), [startedAt, setStartedAt] = useState(0);
  const [mode, setMode] = useState<"setup" | "exam" | "marking" | "results">("setup"), [generating, setGenerating] = useState(false), [error, setError] = useState<string | null>(null), [userId, setUserId] = useState(""), [online, setOnline] = useState(true), [savedAt, setSavedAt] = useState<number | null>(null);
  const [flags, setFlags] = useState<number[]>([]), [panel, setPanel] = useState<"canvas" | "calculator" | "formulae" | "hint" | "upload">("canvas"), [canvasOpen, setCanvasOpen] = useState(true), [mobileNav, setMobileNav] = useState(false), [more, setMore] = useState(false), [confirmSubmit, setConfirmSubmit] = useState(false);
  const [calculator, setCalculator] = useState(""), [calcResult, setCalcResult] = useState<string | null>(null), [hint, setHint] = useState<string | null>(null), [hintLoading, setHintLoading] = useState(false), [attachment, setAttachment] = useState<string | null>(null), [resumeId, setResumeId] = useState<string | null>(null), [results, setResults] = useState<ExamResults | null>(null);
  const workId = useRef<string | null>(null), answersRef = useRef(answers), questionsRef = useRef(questions), currentRef = useRef(current), secondsRef = useRef(seconds), totalRef = useRef(totalSeconds), canvasRef = useRef(""), fileRef = useRef<HTMLInputElement>(null), submitRef = useRef(false);
  const { help } = useCortexVerify();
  useEffect(() => { answersRef.current = answers; }, [answers]); useEffect(() => { questionsRef.current = questions; }, [questions]); useEffect(() => { currentRef.current = current; }, [current]); useEffect(() => { secondsRef.current = seconds; }, [seconds]); useEffect(() => { totalRef.current = totalSeconds; }, [totalSeconds]);
  useEffect(() => { const f = () => setOnline(navigator.onLine); f(); addEventListener("online", f); addEventListener("offline", f); return () => { removeEventListener("online", f); removeEventListener("offline", f); }; }, []);
  useEffect(() => { (async () => { const { data } = await createClient().auth.getUser(); if (data.user) setUserId(data.user.id); })(); }, []);
  useEffect(() => { if (!userId || mode !== "setup") return; try { const raw = localStorage.getItem(`${KEY}:${userId}`); if (raw) { const saved = JSON.parse(raw) as { workId: string; questions: ExamQuestion[] }; if (saved.questions?.length) getStudySession(saved.workId).then((session) => { if (session && (session.status === "active" || session.status === "paused")) setResumeId(saved.workId); }); } } catch {} }, [mode, userId]);
  const persist = useCallback(async () => { if (!workId.current || !questionsRef.current.length || !userId) return; const payload = { questions: questionsRef.current, answers: answersRef.current, current: currentRef.current, seconds: secondsRef.current, totalSeconds: totalRef.current, startedAt, flags, canvas: canvasRef.current }; try { await saveWorkObject({ id: workId.current, mode: "exam" as StudySpaceMode, status: "draft", subject, topic: topic || undefined, prompt: `${subject} exam`, response: JSON.stringify(payload), working: canvasRef.current || undefined, createdAt: new Date(startedAt || Date.now()).toISOString(), updatedAt: new Date().toISOString() }); const session = await getStudySession(workId.current); await saveStudySession(session ? { ...session, updatedAt: new Date().toISOString(), remainingMs: secondsRef.current * 1000 } : createStudySession(workId.current, "exam", secondsRef.current * 1000)); localStorage.setItem(`${KEY}:${userId}`, JSON.stringify({ ...payload, workId: workId.current, subject, topic, level, count })); setSavedAt(Date.now()); } catch {} }, [count, flags, level, startedAt, subject, topic, userId]);
  useEffect(() => { if (mode !== "exam") return; const t = setTimeout(() => void persist(), 600); return () => clearTimeout(t); }, [answers, current, flags, mode, persist, seconds]);
  useEffect(() => { if (mode !== "exam") return; const timer = setInterval(() => setSeconds((s) => Math.max(0, s - 1)), 1000); return () => clearInterval(timer); }, [mode]);
  useEffect(() => { if (mode === "exam" && seconds === 0 && totalSeconds > 0) setConfirmSubmit(true); }, [mode, seconds, totalSeconds]);

  const q = questions[current], answer = answers.find((a) => a.questionId === q?.id)?.answer ?? "", answered = useMemo(() => new Set(answers.filter((a) => a.answer.trim()).map((a) => a.questionId)), [answers]), progress = questions.length ? Math.round(answered.size / questions.length * 100) : 0, danger = seconds <= Math.max(60, totalSeconds * 0.2), formulasForSubject = useMemo(() => formulas(subject), [subject]);
  const setAnswer = (value: string) => { if (!q) return; setAnswers((old) => { const next = [...old], index = next.findIndex((a) => a.questionId === q.id), payload = { questionId: q.id, answer: value, timeSpent: 0 }; if (index >= 0) next[index] = payload; else next.push(payload); return next; }); };
  const go = (index: number) => { if (questions[index]) { setCurrent(index); setHint(null); } };
  const flag = () => q && setFlags((old) => old.includes(q.id) ? old.filter((x) => x !== q.id) : [...old, q.id]);

  const generate = async () => { if (!subject || !online || generating) return; setGenerating(true); setError(null); try { const { data: { session } } = await createClient().auth.getSession(); const res = await fetchWithTimeout("/api/exam/generate", { method: "POST", headers: { "Content-Type": "application/json", ...(session?.access_token ? { Authorization: `Bearer ${session.access_token}` } : {}) }, body: JSON.stringify({ subject, topic: topic ? `${topic} (${LEVELS[level].curriculum})` : `${subject} (${LEVELS[level].curriculum})`, difficulty: LEVELS[level].api, questionCount: count }) }, 100000); const data = await res.json(); if (!res.ok || !Array.isArray(data.questions)) throw new Error(data.error || "No questions were generated."); const total = count * 120; workId.current = `exam:${id()}`; setQuestions(data.questions); setAnswers([]); setFlags([]); setCurrent(0); setTotalSeconds(total); setSeconds(total); setStartedAt(Date.now()); setMode("exam"); setResumeId(null); setPanel("canvas"); } catch (e) { setError(e instanceof FetchTimeoutError ? "Generation took too long. Try again on a stronger connection." : e instanceof Error ? e.message : "Could not generate the exam."); } finally { setGenerating(false); } };
  const resume = async () => { if (!resumeId || !userId) return; try { const raw = localStorage.getItem(`${KEY}:${userId}`); if (!raw) return; const saved = JSON.parse(raw) as { workId: string; subject: string; topic: string; questions: ExamQuestion[]; answers: Answer[]; current: number; seconds: number; totalSeconds: number; startedAt: number; flags: number[]; level: number; count: number }; const session = await getStudySession(saved.workId); if (!session) return; workId.current = saved.workId; setSubject(saved.subject); setTopic(saved.topic); setLevel(saved.level); setCount(saved.count); setQuestions(saved.questions); setAnswers(saved.answers); setCurrent(saved.current); setTotalSeconds(saved.totalSeconds); setSeconds(Math.max(0, Math.round((session.remainingMs ?? saved.seconds * 1000) / 1000))); setStartedAt(saved.startedAt); setFlags(saved.flags || []); setMode("exam"); setResumeId(null); } catch { setError("That saved exam could not be restored."); } };
  const askHint = async () => { if (!q || hintLoading) return; setHintLoading(true); setPanel("hint"); try { const response = await help({ mode: "help", subject, question: q.question, level: "hint" }); setHint(response?.hint || response?.content || (online ? "Cortex could not produce a hint right now." : "You're offline. Keep working on the Canvas and ask Cortex when you reconnect.")); } finally { setHintLoading(false); } };
  const submit = async () => { if (submitRef.current || !questions.length || !online) return; submitRef.current = true; setConfirmSubmit(false); setMode("marking"); try { const { data: { session } } = await createClient().auth.getSession(); const res = await fetchWithTimeout("/api/exam/mark", { method: "POST", headers: { "Content-Type": "application/json", ...(session?.access_token ? { Authorization: `Bearer ${session.access_token}` } : {}) }, body: JSON.stringify({ subject, difficulty: LEVELS[level].curriculum, questions, answers: answersRef.current, timeTaken: totalRef.current - secondsRef.current }) }, 100000); const data = await res.json(); if (!res.ok) throw new Error(data.error || "Marking failed."); setResults(data); if (userId) { const correct = data.results?.filter((r: ExamResult) => r.correct).length ?? 0; await createClient().from("exam_results").insert({ user_id: userId, subject, topic: topic || null, difficulty: LEVELS[level].label, score: data.percentage, total_questions: questions.length, correct_answers: correct, weak_areas: data.weakAreas ?? [], time_taken: totalRef.current - secondsRef.current }); } if (workId.current) { const sessionState = await getStudySession(workId.current); if (sessionState) await saveStudySession({ ...sessionState, status: "submitted", updatedAt: new Date().toISOString(), remainingMs: 0 }); } localStorage.removeItem(`${KEY}:${userId}`); setMode("results"); onFinished?.(data); } catch (e) { setError(e instanceof FetchTimeoutError ? "Marking timed out. Your exam is still saved. Try again when the connection is stable." : e instanceof Error ? e.message : "Marking failed. Your work is still saved."); setMode("exam"); } finally { submitRef.current = false; } };
  const reset = async () => { if (workId.current) await deleteStudySession(workId.current).catch(() => undefined); localStorage.removeItem(`${KEY}:${userId}`); workId.current = null; setMode("setup"); setQuestions([]); setAnswers([]); setFlags([]); setResults(null); setSeconds(0); setTotalSeconds(0); setResumeId(null); };
  const calc = () => { try { if (!/^[0-9+\-*/().%\s]+$/.test(calculator)) throw new Error("Basic arithmetic only"); const value = Function(`"use strict"; return (${calculator})`)(); setCalcResult(Number.isFinite(value) ? String(value) : "Invalid"); } catch (e) { setCalcResult(e instanceof Error ? e.message : "Invalid"); } };
  const attach = (file?: File) => { if (!file || !file.type.startsWith("image/") || file.size > 5 * 1024 * 1024) return; const reader = new FileReader(); reader.onload = () => setAttachment(String(reader.result || "")); reader.readAsDataURL(file); };

  if (mode === "setup") return <div className="min-h-[calc(100vh-4rem)] px-4 py-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-6 flex justify-between gap-4"><div><div className="mb-2 inline-flex items-center gap-2 rounded-full border border-cyan-500/20 bg-cyan-500/10 px-3 py-1.5 text-xs font-bold uppercase tracking-wider text-cyan-500"><Sparkles size={13} /> Exam Simulation</div><h1 className="text-3xl font-black tracking-tight sm:text-4xl">Build your exam room.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted-foreground)]">A realistic exam workstation with a paper view, Canvas, timer, marking, Cortex help and offline-safe autosave.</p></div><button onClick={onExit} className="hidden h-fit rounded-xl border border-[var(--card-border)] px-4 py-2.5 text-sm font-bold sm:block">Exit</button></div>{resumeId && <div className="mb-5 flex items-center justify-between gap-3 rounded-2xl border border-cyan-500/20 bg-cyan-500/5 p-4"><div className="flex gap-3"><Save className="text-cyan-500" size={19} /><div><p className="text-sm font-bold">Unfinished exam saved</p><p className="mt-1 text-xs text-[var(--muted-foreground)]">Resume it even without a connection.</p></div></div><button onClick={() => void resume()} className="rounded-xl bg-cyan-500 px-4 py-2.5 text-sm font-black text-slate-950">Resume</button></div>}<div className="grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><section className="rounded-3xl border border-[var(--card-border)] bg-[var(--card)] p-5 sm:p-7"><p className="text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Exam blueprint</p><h2 className="mt-1 text-xl font-black">Choose your paper</h2><label className="mt-6 block text-sm font-bold">Subject<select value={subject} onChange={(e) => setSubject(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--card-border)] bg-[var(--muted)] px-3.5 py-3 text-sm"><option value="">Choose a subject…</option>{SUBJECTS.map((s) => <option key={s}>{s}</option>)}</select></label><label className="mt-4 block text-sm font-bold">Topic <span className="font-normal text-[var(--muted-foreground)]">optional</span><input value={topic} onChange={(e) => setTopic(e.target.value)} placeholder="e.g. mechanics, calculus, organic chemistry" className="mt-2 w-full rounded-xl border border-[var(--card-border)] bg-[var(--muted)] px-3.5 py-3 text-sm" /></label><p className="mt-5 text-sm font-bold">Level</p><div className="mt-2 grid gap-2 sm:grid-cols-3">{LEVELS.map((item, i) => <button key={item.label} onClick={() => setLevel(i)} className={`rounded-2xl border p-4 text-left ${level === i ? "border-cyan-500/50 bg-cyan-500/10" : "border-[var(--card-border)] bg-[var(--muted)]"}`}><div className="flex justify-between text-sm font-black">{item.label}<span className="h-2.5 w-2.5 rounded-full" style={{ background: item.accent }} /></div><p className="mt-1 text-xs text-[var(--muted-foreground)]">{item.curriculum}</p></button>)}</div><p className="mt-5 text-sm font-bold">Questions</p><div className="mt-2 grid grid-cols-4 gap-2">{COUNTS.map((n) => <button key={n} onClick={() => setCount(n)} className={`rounded-xl border py-3 text-sm font-black ${count === n ? "border-cyan-500/50 bg-cyan-500/10 text-cyan-500" : "border-[var(--card-border)] bg-[var(--muted)]"}`}>{n}</button>)}</div>{error && <div className="mt-4 rounded-2xl border border-red-500/20 bg-red-500/5 p-4 text-sm">{error}</div>}{!online && <div className="mt-4 flex gap-3 rounded-2xl bg-[var(--muted)] p-4 text-xs"><CloudOff size={18} /><span>Offline: saved exams can resume, but a new AI paper needs a connection.</span></div>}<button disabled={!subject || !online || generating} onClick={() => void generate()} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-cyan-500 px-5 py-4 text-sm font-black text-slate-950 disabled:opacity-50">{generating ? <><Loader2 size={18} className="animate-spin" /> Cortex is building your paper…</> : <>Enter exam room <ArrowRight size={18} /></>}</button></section><div className="space-y-3">{[[Clock3, "Real exam pacing", "Visible timer, question timing and review controls."], [Pencil, "Working matters", "Use Canvas for calculations, diagrams and rough work."], [Sparkles, "Cortex after the paper", "Weak areas, strong areas and next revision moves."], [CloudOff, "Offline-safe", "Answers and session state stay on this device when the network drops."]].map(([Icon, title, text]) => <div key={String(title)} className="rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4"><Icon size={18} className="text-cyan-500" /><p className="mt-3 text-sm font-black">{String(title)}</p><p className="mt-1 text-xs leading-5 text-[var(--muted-foreground)]">{String(text)}</p></div>)}</div></div></div></div>;
  if (mode === "marking") return <div className="flex min-h-[70vh] items-center justify-center text-center"><div><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-cyan-500/10 text-cyan-500"><Loader2 className="animate-spin" size={30} /></div><h2 className="mt-5 text-2xl font-black">Cortex is marking your paper</h2><p className="mt-2 text-sm text-[var(--muted-foreground)]">Checking answers and preparing your learning feedback.</p></div></div>;
  if (mode === "results" && results) return <Results results={results} subject={subject} topic={topic} onNew={() => void reset()} onExit={onExit} />;
  if (!q) return null;
  const qNo = current + 1, flagged = flags.includes(q.id), timerPercent = totalSeconds ? seconds / totalSeconds * 100 : 100;

  return <div className="min-h-screen"><header className="sticky top-0 z-40 border-b border-[var(--card-border)] bg-[var(--background)]/95 backdrop-blur"><div className="mx-auto flex max-w-[1800px] items-center gap-3 px-3 py-3 sm:px-5"><button onClick={() => setMobileNav(true)} className="rounded-xl border border-[var(--card-border)] p-2 lg:hidden"><Menu size={18} /></button><button onClick={onExit} className="hidden items-center gap-1 rounded-xl px-2 py-2 text-sm font-bold text-[var(--muted-foreground)] lg:flex"><ArrowLeft size={16} /> Exam Simulation</button><div className="hidden h-5 w-px bg-[var(--card-border)] lg:block" /><div className="min-w-0 flex-1"><div className="flex items-center gap-2 text-sm font-bold"><span className="truncate">{subject}</span><ChevronRight size={14} /><span className="truncate text-[var(--muted-foreground)]">{topic || "Mixed topics"}</span></div><p className="text-[11px] text-[var(--muted-foreground)]">{LEVELS[level].label} · Paper 1 · {questions.length} questions</p></div><span className={`hidden items-center gap-2 rounded-xl border px-3 py-2 text-xs font-bold sm:flex ${online ? "border-emerald-500/20 text-emerald-500" : "border-amber-500/20 text-amber-500"}`}>{online ? <Wifi size={14} /> : <CloudOff size={14} />}{online ? "Online" : "Offline"}</span><span className={`flex items-center gap-2 rounded-xl border px-3 py-2 font-mono text-sm font-black ${danger ? "border-red-500/30 text-red-500" : "border-[var(--card-border)]"}`}><Clock3 size={15} />{time(seconds)}</span><button onClick={() => setConfirmSubmit(true)} className="hidden rounded-xl bg-cyan-500 px-4 py-2.5 text-xs font-black text-slate-950 sm:block">Submit</button></div></header>
  <div className="mx-auto grid max-w-[1800px] gap-3 p-3 sm:p-5 lg:grid-cols-[220px_minmax(480px,1fr)_300px]"><aside className="hidden rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-3 lg:block"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Question paper</p><p className="mt-1 text-sm font-black">Paper 1</p></div><Grid2X2 size={16} className="text-cyan-500" /></div><div className="mt-3 max-h-[calc(100vh-180px)] overflow-auto rounded-xl bg-[var(--muted)] p-2">{questions.map((question, i) => <button key={question.id} onClick={() => go(i)} className={`flex w-full items-center gap-2 rounded-xl p-2.5 text-left ${i === current ? "bg-cyan-500 text-slate-950" : "hover:bg-[var(--card)]"}`}><span className={`flex h-7 w-7 items-center justify-center rounded-lg text-xs font-black ${i === current ? "bg-white/30" : answered.has(question.id) ? "bg-emerald-500/15 text-emerald-500" : "bg-[var(--card)]"}`}>{i + 1}</span><span className="min-w-0 flex-1 truncate text-xs font-semibold">{question.topic}</span>{flags.includes(question.id) && <Flag size={12} fill="currentColor" />}</button>)}</div></aside>
  <main className="min-w-0"><div className="mb-3 flex flex-wrap items-center gap-1 rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-2"><ToolButton active={panel === "canvas"} onClick={() => { setPanel("canvas"); setCanvasOpen(true); }} icon={<Pencil size={14} />} label="Canvas" /><ToolButton active={panel === "calculator"} onClick={() => setPanel("calculator")} icon={<Calculator size={14} />} label="Calculator" /><ToolButton active={panel === "formulae"} onClick={() => setPanel("formulae")} icon={<BookOpen size={14} />} label="Formulae" /><ToolButton active={panel === "hint"} onClick={() => void askHint()} icon={<Lightbulb size={14} />} label="AI Hint" /><ToolButton active={panel === "upload"} onClick={() => { setPanel("upload"); fileRef.current?.click(); }} icon={<Paperclip size={14} />} label="Upload" /><div className="ml-auto flex gap-1"><button onClick={flag} className={`rounded-xl p-2.5 ${flagged ? "bg-amber-500/10 text-amber-500" : "hover:bg-[var(--muted)]"}`}>{flagged ? <Flag size={15} fill="currentColor" /> : <FlagOff size={15} />}</button><button onClick={() => setMore((v) => !v)} className="rounded-xl p-2.5 hover:bg-[var(--muted)]"><MoreHorizontal size={17} /></button></div></div>{more && <div className="mb-3 flex flex-wrap gap-2 rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-3"><button onClick={() => setCanvasOpen((v) => !v)} className="rounded-xl px-3 py-2 text-xs font-bold hover:bg-[var(--muted)]">Toggle Canvas</button><button onClick={() => document.documentElement.requestFullscreen?.()} className="rounded-xl px-3 py-2 text-xs font-bold hover:bg-[var(--muted)]">Fullscreen</button><button onClick={() => setConfirmSubmit(true)} className="rounded-xl px-3 py-2 text-xs font-bold text-red-500">End exam</button></div>}
  <div className={`grid gap-3 ${canvasOpen ? "xl:grid-cols-2" : "grid-cols-1"}`}><section className="min-h-[570px] overflow-hidden rounded-2xl border border-slate-200 bg-[#f8fafc] text-slate-900"><div className="flex items-center justify-between border-b border-slate-200 bg-white px-4 py-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-slate-500">{subject}</p><p className="text-sm font-black">Question {qNo} <span className="font-normal text-slate-500">of {questions.length}</span></p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold">{q.marks} mark{q.marks === 1 ? "" : "s"}</span></div><div className="max-h-[calc(100vh-300px)] overflow-auto p-5 sm:p-7"><div className="mb-4 text-[10px] font-bold uppercase tracking-wider text-slate-500">{q.type.replace("_", " ")} · {q.topic}</div><div className="text-sm leading-7" dangerouslySetInnerHTML={{ __html: math(q.question) }} />{q.options && <div className="mt-7 space-y-2">{q.options.map((option, i) => <button key={option} onClick={() => setAnswer(option)} className={`flex w-full items-center gap-3 rounded-xl border p-3.5 text-left text-sm ${answer === option ? "border-cyan-500 bg-cyan-50" : "border-slate-200 bg-white"}`}><span className={`flex h-7 w-7 items-center justify-center rounded-full border text-xs font-black ${answer === option ? "border-cyan-500 bg-cyan-500 text-white" : "border-slate-300"}`}>{String.fromCharCode(65 + i)}</span><span dangerouslySetInnerHTML={{ __html: math(option) }} /></button>)}</div>}</div></section>{canvasOpen && <section className="overflow-hidden rounded-2xl border border-[var(--card-border)] bg-[var(--card)]"><div className="flex items-center justify-between border-b border-[var(--card-border)] px-4 py-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Your workspace</p><p className="text-sm font-black">Working & answer</p></div><span className="rounded-lg bg-cyan-500/10 px-2 py-1 text-[10px] font-bold text-cyan-500">{savedAt ? "Autosaved" : "Saving…"}</span></div><div className="p-3"><StudyCanvas storageKey={`${KEY}:${userId}:${workId.current ?? "new"}:${q.id}`} height={470} onChange={(data) => { canvasRef.current = data; void persist(); }} /><label className="mt-4 block text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Final answer<textarea value={answer} onChange={(e) => setAnswer(e.target.value)} rows={4} placeholder="Enter the answer you want Cortex to mark…" className="mt-2 w-full resize-y rounded-xl border border-[var(--card-border)] bg-[var(--muted)] p-3 text-sm leading-6" /></label>{attachment && <div className="mt-3 flex items-center gap-3 rounded-xl border border-[var(--card-border)] p-3"><img src={attachment} alt="Attached working" className="h-14 w-14 rounded-lg object-cover" /><span className="text-xs font-bold">Working image attached</span><button onClick={() => setAttachment(null)} className="ml-auto"><X size={15} /></button></div>}</div></section>}</div>
  {panel === "calculator" && <Panel title="Calculator" onClose={() => setPanel("canvas")}><div className="flex gap-2"><input value={calculator} onChange={(e) => setCalculator(e.target.value)} onKeyDown={(e) => e.key === "Enter" && calc()} placeholder="e.g. (3.5*8)/2" className="min-w-0 flex-1 rounded-xl border border-[var(--card-border)] bg-[var(--muted)] px-3 py-3 text-sm" /><button onClick={calc} className="rounded-xl bg-cyan-500 px-4 font-black text-slate-950">=</button></div>{calcResult !== null && <p className="mt-3 rounded-xl bg-[var(--muted)] p-3 font-mono text-sm">{calcResult}</p>}</Panel>}
  {panel === "formulae" && <Panel title={`${subject} formulae`} onClose={() => setPanel("canvas")}><div className="grid gap-2 sm:grid-cols-2">{formulasForSubject.map((f) => <div key={f} className="rounded-xl border border-[var(--card-border)] bg-[var(--muted)] p-3 text-sm font-semibold">{f}</div>)}</div></Panel>}
  {panel === "hint" && <Panel title="Cortex Hint" onClose={() => setPanel("canvas")}>{hintLoading ? <div className="flex gap-2 text-sm"><Loader2 size={16} className="animate-spin text-cyan-500" /> Cortex is thinking…</div> : <div className="rounded-xl border border-cyan-500/20 bg-cyan-500/5 p-4 text-sm leading-6">{hint || "Ask Cortex for the next useful step without revealing the full answer."}</div>}</Panel>}
  {panel === "upload" && <Panel title="Attach working" onClose={() => setPanel("canvas")}><div className="text-center"><Paperclip className="mx-auto text-cyan-500" size={22} /><p className="mt-2 text-sm font-bold">Add a photo of your working</p><button onClick={() => fileRef.current?.click()} className="mt-3 rounded-xl bg-[var(--muted)] px-4 py-2.5 text-xs font-black">Choose image</button></div></Panel>}<input ref={fileRef} type="file" accept="image/*" capture="environment" className="hidden" onChange={(e) => attach(e.target.files?.[0])} />
  {error && <div className="mt-3 rounded-2xl border border-amber-500/20 bg-amber-500/5 p-4 text-sm">{error}</div>}<div className="mt-3 flex items-center justify-between rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-2.5"><button disabled={current === 0} onClick={() => go(current - 1)} className="inline-flex items-center gap-2 rounded-xl px-3 py-2.5 text-xs font-black disabled:opacity-30"><ArrowLeft size={15} /> Previous</button><span className="hidden text-[11px] text-[var(--muted-foreground)] sm:block">Q{qNo}/{questions.length} · {answered.has(q.id) ? "Answered" : "Not answered"}</span><button onClick={() => current === questions.length - 1 ? setConfirmSubmit(true) : go(current + 1)} className="inline-flex items-center gap-2 rounded-xl bg-cyan-500 px-4 py-2.5 text-xs font-black text-slate-950">{current === questions.length - 1 ? "Review & submit" : "Next"}<ArrowRight size={15} /></button></div><div className="mt-2 flex justify-between px-1 text-[11px] text-[var(--muted-foreground)]"><span>{online ? "Online · changes saved locally" : "Offline · working safely saved"}</span><span>{savedAt ? `Saved ${new Date(savedAt).toLocaleTimeString()}` : "Autosaving…"}</span></div></main>
  <aside className="hidden space-y-3 lg:block"><div className="rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4"><div className="flex justify-between text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]"><span>Time remaining</span><Clock3 size={15} className={danger ? "text-red-500" : "text-cyan-500"} /></div><div className={`mt-3 text-center font-mono text-4xl font-black ${danger ? "text-red-500" : ""}`}>{time(seconds)}</div><div className="mt-3 h-2 overflow-hidden rounded-full bg-[var(--muted)]"><div className={`h-full rounded-full ${danger ? "bg-red-500" : "bg-cyan-500"}`} style={{ width: `${timerPercent}%` }} /></div><p className="mt-2 text-center text-[11px] text-[var(--muted-foreground)]">of {time(totalSeconds)}</p></div><div className="rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Question navigator</p><p className="mt-1 text-sm font-black">{answered.size}/{questions.length} answered</p></div><Grid2X2 size={16} className="text-cyan-500" /></div><div className="mt-4 grid grid-cols-5 gap-2">{questions.map((question, i) => <button key={question.id} onClick={() => go(i)} className={`relative h-9 rounded-lg text-xs font-black ${i === current ? "bg-cyan-500 text-slate-950" : answered.has(question.id) ? "bg-emerald-500/15 text-emerald-500" : "bg-[var(--muted)]"}`}>{i + 1}{flags.includes(question.id) && <span className="absolute -right-1 -top-1 h-2 w-2 rounded-full bg-amber-500" />}</button>)}</div><button onClick={() => setConfirmSubmit(true)} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl border border-cyan-500/30 bg-cyan-500/5 px-3 py-2.5 text-xs font-black text-cyan-500"><Check size={15} /> Review & submit</button></div><div className="rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4"><div className="flex justify-between text-xs font-bold"><span>Progress</span><span className="text-cyan-500">{progress}%</span></div><div className="mt-3 h-2 rounded-full bg-[var(--muted)]"><div className="h-full rounded-full bg-cyan-500" style={{ width: `${progress}%` }} /></div><div className="mt-3 grid grid-cols-2 gap-2 text-center"><Stat label="Answered" value={String(answered.size)} /><Stat label="Flagged" value={String(flags.length)} /></div></div></aside></div>
  {confirmSubmit && <Modal title="Submit exam?" onClose={() => setConfirmSubmit(false)}><div className="space-y-4"><div className="grid grid-cols-3 gap-2"><Stat label="Answered" value={`${answered.size}/${questions.length}`} /><Stat label="Flagged" value={String(flags.length)} /><Stat label="Time" value={time(totalSeconds - seconds)} /></div>{!online && <p className="rounded-xl bg-amber-500/5 p-3 text-xs">You're offline. Keep working and reconnect before submitting for marking.</p>}<div className="flex gap-2"><button onClick={() => setConfirmSubmit(false)} className="flex-1 rounded-xl border border-[var(--card-border)] px-4 py-3 text-sm font-bold">Keep working</button><button onClick={() => void submit()} disabled={!online} className="flex-1 rounded-xl bg-cyan-500 px-4 py-3 text-sm font-black text-slate-950 disabled:opacity-40">Submit for marking</button></div></div></Modal>}
  {mobileNav && <div className="fixed inset-0 z-50 lg:hidden"><button onClick={() => setMobileNav(false)} className="absolute inset-0 bg-black/50" /><div className="absolute inset-y-0 left-0 w-[88%] max-w-sm overflow-auto bg-[var(--card)] p-4"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]">Question paper</p><p className="mt-1 text-lg font-black">{subject}</p></div><button onClick={() => setMobileNav(false)}><X size={18} /></button></div><div className="mt-4 space-y-1">{questions.map((question, i) => <button key={question.id} onClick={() => { go(i); setMobileNav(false); }} className={`flex w-full items-center gap-3 rounded-xl p-3 text-left ${i === current ? "bg-cyan-500 text-slate-950" : "hover:bg-[var(--muted)]"}`}><span className="flex h-8 w-8 items-center justify-center rounded-lg text-xs font-black">{i + 1}</span><span className="flex-1 text-sm font-semibold">{question.topic}</span>{flags.includes(question.id) && <Flag size={14} fill="currentColor" />}</button>)}</div></div></div>}
  </div>;
}

function ToolButton({ active, onClick, icon, label }: { active: boolean; onClick: () => void; icon: ReactNode; label: string }) { return <button onClick={onClick} className={`inline-flex items-center gap-2 rounded-xl px-3 py-2 text-xs font-black ${active ? "bg-cyan-500 text-slate-950" : "hover:bg-[var(--muted)]"}`}>{icon}{label}</button>; }
function Panel({ title, children, onClose }: { title: string; children: ReactNode; onClose: () => void }) { return <div className="mt-3 rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4"><div className="mb-3 flex justify-between"><p className="text-sm font-black">{title}</p><button onClick={onClose}><X size={15} /></button></div>{children}</div>; }
function Modal({ title, children, onClose }: { title: string; children: ReactNode; onClose: () => void }) { return <div className="fixed inset-0 z-[100] flex items-end justify-center bg-black/50 p-3 sm:items-center"><div className="w-full max-w-md rounded-3xl border border-[var(--card-border)] bg-[var(--card)] p-5 shadow-2xl"><div className="flex justify-between"><h2 className="text-lg font-black">{title}</h2><button onClick={onClose}><X size={18} /></button></div><div className="mt-4">{children}</div></div></div>; }
function Stat({ label, value }: { label: string; value: string }) { return <div className="rounded-xl bg-[var(--muted)] p-3 text-center"><p className="text-lg font-black">{value}</p><p className="text-[10px] text-[var(--muted-foreground)]">{label}</p></div>; }
function Results({ results, subject, topic, onNew, onExit }: { results: ExamResults; subject: string; topic: string; onNew: () => void; onExit?: () => void }) { const [tab, setTab] = useState<"overview" | "review">("overview"), [open, setOpen] = useState<number | null>(null); const pct = Math.round(results.percentage), g = results.grade || grade(pct); return <div className="min-h-[calc(100vh-4rem)] px-4 py-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-5xl"><div className="flex justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-cyan-500">Exam complete</p><h1 className="mt-1 text-3xl font-black">Your paper has been marked.</h1><p className="mt-2 text-sm text-[var(--muted-foreground)]">{subject}{topic ? ` · ${topic}` : ""}</p></div><button onClick={onExit} className="hidden rounded-xl border border-[var(--card-border)] px-4 py-2.5 text-sm font-bold sm:block">Exit</button></div><div className="mt-6 grid gap-4 lg:grid-cols-2"><div className="rounded-3xl border border-[var(--card-border)] bg-[var(--card)] p-7 text-center"><div className="mx-auto flex h-28 w-28 items-center justify-center rounded-full border-8 border-cyan-500/15"><div><p className="text-4xl font-black text-cyan-500">{pct}%</p><p className="text-xs font-bold text-[var(--muted-foreground)]">Grade {g}</p></div></div><div className="mt-6 grid grid-cols-3 divide-x divide-[var(--card-border)]"><div><p className="text-xl font-black">{results.totalScore}/{results.maxScore}</p><p className="text-[10px] text-[var(--muted-foreground)]">Score</p></div><div><p className="text-xl font-black">{Math.round(results.timeTaken / 60)}m</p><p className="text-[10px] text-[var(--muted-foreground)]">Time</p></div><div><p className="text-xl font-black">{results.results.length}</p><p className="text-[10px] text-[var(--muted-foreground)]">Questions</p></div></div></div><div className="space-y-3"><Insight title="Strong areas" items={results.strongAreas} good /><Insight title="Needs work" items={results.weakAreas} /><div className="rounded-2xl border border-cyan-500/20 bg-cyan-500/5 p-4"><div className="flex items-center gap-2 text-sm font-black"><Sparkles size={16} className="text-cyan-500" /> Cortex insight</div><p className="mt-2 text-sm leading-6 text-[var(--muted-foreground)]">{results.cortexInsight || "Your result is now part of the learning loop."}</p></div></div></div><div className="mt-5 rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-2"><div className="grid grid-cols-2 gap-2"><button onClick={() => setTab("overview")} className={`rounded-xl px-4 py-3 text-sm font-black ${tab === "overview" ? "bg-[var(--muted)]" : "text-[var(--muted-foreground)]"}`}>Topic overview</button><button onClick={() => setTab("review")} className={`rounded-xl px-4 py-3 text-sm font-black ${tab === "review" ? "bg-[var(--muted)]" : "text-[var(--muted-foreground)]"}`}>Review questions</button></div></div>{tab === "overview" ? <div className="mt-3 rounded-2xl border border-[var(--card-border)] bg-[var(--card)] p-4">{Object.entries(results.results.reduce<Record<string, { score: number; max: number }>>((acc, item) => { const key = item.topic || "General"; acc[key] ??= { score: 0, max: 0 }; acc[key].score += item.score; acc[key].max += item.maxScore; return acc; }, {})).map(([key, v]) => { const p = v.max ? Math.round(v.score / v.max * 100) : 0; return <div key={key} className="border-b border-[var(--card-border)] py-3 last:border-0"><div className="flex justify-between text-sm"><span className="font-bold">{key}</span><span className="font-black text-cyan-500">{p}%</span></div><div className="mt-2 h-2 rounded-full bg-[var(--muted)]"><div className="h-full rounded-full bg-cyan-500" style={{ width: `${p}%` }} /></div></div>; })}</div> : <div className="mt-3 space-y-2">{results.results.map((item, i) => <div key={`${item.questionId}-${i}`} className="rounded-2xl border border-[var(--card-border)] bg-[var(--card)]"><button onClick={() => setOpen(open === i ? null : i)} className="flex w-full items-center gap-3 p-4 text-left"><span className={item.correct ? "text-emerald-500" : "text-red-500"}>{item.correct ? <CheckCircle2 size={18} /> : <AlertTriangle size={18} />}</span><span className="flex-1"><span className="block text-sm font-black">Question {i + 1} · {item.topic}</span><span className="text-xs text-[var(--muted-foreground)]">{item.score}/{item.maxScore} marks</span></span><ChevronDown size={16} /></button>{open === i && <div className="border-t border-[var(--card-border)] p-4 text-sm leading-6"><p className="font-bold">Feedback</p><p className="mt-1 text-[var(--muted-foreground)]">{item.feedback}</p>{item.modelAnswer && <><p className="mt-4 font-bold">Model answer</p><p className="mt-1 text-[var(--muted-foreground)]">{item.modelAnswer}</p></>}</div>}</div>)}</div>}<div className="mt-6 flex gap-2"><button onClick={onNew} className="flex-1 rounded-2xl border border-[var(--card-border)] px-4 py-3.5 text-sm font-black"><RotateCcw size={16} className="mr-2 inline" />New exam</button><button onClick={onNew} className="flex-1 rounded-2xl bg-cyan-500 px-4 py-3.5 text-sm font-black text-slate-950"><Zap size={16} className="mr-2 inline" />Retake & improve</button></div></div></div>; }
function Insight({ title, items, good = false }: { title: string; items: string[]; good?: boolean }) { return <div className={`rounded-2xl border p-4 ${good ? "border-emerald-500/20 bg-emerald-500/5" : "border-amber-500/20 bg-amber-500/5"}`}><p className={`text-xs font-black uppercase tracking-wider ${good ? "text-emerald-500" : "text-amber-500"}`}>{title}</p><div className="mt-2 flex flex-wrap gap-2">{items?.length ? items.map((x) => <span key={x} className="rounded-full bg-[var(--card)] px-3 py-1.5 text-xs font-semibold">{x}</span>) : <span className="text-xs text-[var(--muted-foreground)]">None flagged.</span>}</div></div>; }
