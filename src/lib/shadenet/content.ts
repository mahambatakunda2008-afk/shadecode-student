export type ShadeNetResourceType = "lesson" | "video" | "question-set" | "flashcards" | "study-pack" | "other";
export interface ShadeNetResourceMetadata { type: ShadeNetResourceType; title: string; subject?: string; qualification?: string; syllabus?: string; version?: string; source?: string; authorOrPublisher?: string; verificationStatus?: "official" | "teacher-verified" | "community" | "ai-generated" | "unverified"; createdAt: string; }
export interface ShadeNetResource<T = unknown> { contentHash: string; metadata: ShadeNetResourceMetadata; content: T; }
function canonicalize(value: unknown): string { if (value === null || typeof value !== "object") return JSON.stringify(value); if (Array.isArray(value)) return `[${value.map(canonicalize).join(",")}]`; const object = value as Record<string, unknown>; return `{${Object.keys(object).sort().map(k => `${JSON.stringify(k)}:${canonicalize(object[k])}`).join(",`)}}`; }
function bytesToHex(buffer: ArrayBuffer): string { return [...new Uint8Array(buffer)].map(b => b.toString(16).padStart(2, "0")).join(""); }
export async function contentHash<T>(resource: Omit<ShadeNetResource<T>, "contentHash">): Promise<string> { if (typeof crypto === "undefined" || !crypto.subtle) throw new Error("Web Crypto is required for ShadeNet content addressing"); const digest = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(canonicalize({ metadata: resource.metadata, content: resource.content }))); return bytesToHex(digest); }
export async function createResource<T>(resource: Omit<ShadeNetResource<T>, "contentHash">): Promise<ShadeNetResource<T>> { return { ...resource, contentHash: await contentHash(resource) }; }
